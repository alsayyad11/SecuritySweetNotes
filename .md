# Advanced Database Tasks

## ⪢ Variable Declaration

### 1. Basic Variable Declaration
```sql
DECLARE @EmployeeName VARCHAR(100) = 'Ahmed Elsayyad'
DECLARE @Salary DECIMAL(10,2) = 7500.00
DECLARE @IsActive BIT = 1
```
### 2. Multiple Variables
```sql
DECLARE @Count INT = 1, 
        @Price DECIMAL(10,2) = 2000,
        @ProductName VARCHAR(50) = 'Laptop',
        @IsAvailable BIT = 1,
        @CreatedDate DATE = GETDATE()
```

### 3. Variables from SELECT
```sql
DECLARE @TotalEmployees INT,
        @AverageSalary DECIMAL(10,2)

SELECT @TotalEmployees = COUNT(*),
       @AverageSalary = AVG(Salary)
FROM Employees 
WHERE Department = 'IT'
```

### 4. Secure Variable Initialization
```sql
DECLARE @UserName NVARCHAR(50) = 'Mohammed_Elsayyad'
DECLARE @HashedPassword VARBINARY(256) = HASHBYTES('SHA2_256', 'SecurePassword123!')
DECLARE @LoginAttempts INT = 0
DECLARE @LastLogin DATETIME = NULL
```

### 5. Variables for Security Checks
```sql
DECLARE @CurrentUser NVARCHAR(50) = SYSTEM_USER
DECLARE @DatabaseName NVARCHAR(50) = DB_NAME()
DECLARE @IsAdmin BIT = IS_MEMBER('db_owner')
DECLARE @ConnectionTime DATETIME = GETDATE()
```

## ⪢ SET vs SELECT

### 1. SET with Scalar Value
```sql
DECLARE @EmployeeCount INT
SET @EmployeeCount = (SELECT COUNT(*) FROM Employees WHERE Active = 1)
PRINT 'Active employees: ' + CAST(@EmployeeCount AS VARCHAR)
```

### 2. SELECT with Single Value
```sql
DECLARE @MaxSalary DECIMAL(10,2)
SELECT @MaxSalary = MAX(Salary) FROM Employees
SELECT @MaxSalary AS HighestSalary
```

### 3. SET with Complex Calculation
```sql
DECLARE @TaxAmount DECIMAL(10,2)
SET @TaxAmount = (SELECT SUM(Salary * 0.15) FROM Employees WHERE Salary > 5000)
PRINT 'Total tax amount: ' + CAST(@TaxAmount AS VARCHAR)
```

### 4. SELECT with Multiple Assignments
```sql
DECLARE @ITCount INT, @HRCount INT, @FinanceCount INT

SELECT @ITCount = COUNT(*) FROM Employees WHERE Department = 'IT'
SELECT @HRCount = COUNT(*) FROM Employees WHERE Department = 'HR'  
SELECT @FinanceCount = COUNT(*) FROM Employees WHERE Department = 'Finance'

SELECT @ITCount AS IT_Employees, @HRCount AS HR_Employees, @FinanceCount AS Finance_Employees
```

### 5. Secure User Validation
```sql
DECLARE @UserExists BIT = 0
DECLARE @UserId INT

SELECT @UserExists = 1, 
       @UserId = UserId
FROM Users 
WHERE Username = 'Ahmed_Elsayyad' 
  AND HashedPassword = HASHBYTES('SHA2_256', @InputPassword)
```

## ⪢ IF Conditions

### 1. Basic Age Validation
```sql
DECLARE @Age INT = 25
DECLARE @UserName VARCHAR(100) = 'Mohammed Elsayyad'

IF @Age >= 18
    PRINT @UserName + ' is an adult'
ELSE
    PRINT @UserName + ' is underage'
```

### 2. Salary Grade Classification
```sql
DECLARE @Salary DECIMAL(10,2) = 6500.00
DECLARE @EmployeeName VARCHAR(100) = 'Ahmed Elsayyad'

IF @Salary >= 8000
    PRINT @EmployeeName + ' - High salary grade'
ELSE IF @Salary >= 4000
    PRINT @EmployeeName + ' - Medium salary grade'  
ELSE
    PRINT @EmployeeName + ' - Low salary grade'
```

### 3. User Access Control
```sql
DECLARE @UserRole VARCHAR(50) = 'Manager'
DECLARE @RequestedAction VARCHAR(50) = 'DELETE'

IF @UserRole = 'Admin' OR (@UserRole = 'Manager' AND @RequestedAction != 'DELETE')
    PRINT 'Access granted for ' + @RequestedAction
ELSE
    PRINT 'Access denied - Insufficient privileges'
```

### 4. Data Validation Check
```sql
DECLARE @Email VARCHAR(100) = 'ahmed.elsayyad@company.com'
DECLARE @Phone VARCHAR(15) = '01234567890'

IF @Email LIKE '%@%.%' AND LEN(@Phone) = 11
    PRINT 'Contact information is valid'
ELSE
    PRINT 'Invalid contact information'
```

### 5. Security Breach Detection
```sql
DECLARE @FailedLogins INT = 3
DECLARE @LastFailedAttempt DATETIME = DATEADD(MINUTE, -5, GETDATE())
DECLARE @UserName VARCHAR(100) = 'Mohammed_Elsayyad'

IF @FailedLogins >= 3 AND @LastFailedAttempt > DATEADD(MINUTE, -10, GETDATE())
BEGIN
    PRINT 'Security alert: Multiple failed login attempts for ' + @UserName
    UPDATE Users SET IsLocked = 1 WHERE Username = @UserName
END
```

## ⪢ EXISTS Method

### 1. Check Employee Existence
```sql
DECLARE @EmployeeId INT = 101
DECLARE @EmployeeName VARCHAR(100)

IF EXISTS(SELECT 1 FROM Employees WHERE EmployeeId = @EmployeeId AND Active = 1)
BEGIN
    SELECT @EmployeeName = FirstName + ' ' + LastName 
    FROM Employees 
    WHERE EmployeeId = @EmployeeId
    
    PRINT 'Employee found: ' + @EmployeeName
END
ELSE
    PRINT 'Employee not found or inactive'
```

### 2. Validate Department Before Insert
```sql
DECLARE @DeptName VARCHAR(50) = 'Cybersecurity'
DECLARE @ManagerName VARCHAR(100) = 'Ahmed Elsayyad'

IF NOT EXISTS(SELECT 1 FROM Departments WHERE DepartmentName = @DeptName)
BEGIN
    INSERT INTO Departments (DepartmentName, Manager, CreatedDate)
    VALUES (@DeptName, @ManagerName, GETDATE())
    PRINT 'New department created: ' + @DeptName
END
ELSE
    PRINT 'Department already exists'
```

### 3. Security - Check Duplicate Users
```sql
DECLARE @Username NVARCHAR(50) = 'Mohammed_Elsayyad'
DECLARE @Email NVARCHAR(100) = 'mohammed@company.com'

IF EXISTS(SELECT 1 FROM Users WHERE Username = @Username OR Email = @Email)
    PRINT 'Username or email already exists - Registration failed'
ELSE
BEGIN
    INSERT INTO Users (Username, Email, HashedPassword, CreatedDate)
    VALUES (@Username, @Email, HASHBYTES('SHA2_256', 'TempPassword123'), GETDATE())
    PRINT 'User registered successfully'
END
```

### 4. Data Integrity Check
```sql
DECLARE @ProductId INT = 2001
DECLARE @NewPrice DECIMAL(10,2) = 1500.00

IF EXISTS(SELECT 1 FROM Products WHERE ProductId = @ProductId AND Discontinued = 0)
BEGIN
    UPDATE Products SET Price = @NewPrice, LastUpdated = GETDATE()
    WHERE ProductId = @ProductId
    PRINT 'Price updated successfully'
END
ELSE
    PRINT 'Product not found or discontinued'
```

### 5. Audit Trail Check
```sql
DECLARE @TableName VARCHAR(50) = 'Employees'
DECLARE @ActionType VARCHAR(20) = 'DELETE'

IF EXISTS(SELECT 1 FROM AuditConfiguration WHERE TableName = @TableName AND LogDeletes = 1)
BEGIN
    PRINT 'Audit logging enabled for ' + @ActionType + ' operations on ' + @TableName
    -- Log the audit trail
END
ELSE
    PRINT 'Audit logging not required'
```

## ⪢ WHILE Loops

### 1. Basic Counter Loop
```sql
DECLARE @Counter INT = 1
DECLARE @MaxRecords INT = (SELECT COUNT(*) FROM Employees)

WHILE @Counter <= @MaxRecords
BEGIN
    PRINT 'Processing record: ' + CAST(@Counter AS VARCHAR)
    SET @Counter = @Counter + 1
END
```

### 2. Password Reset Loop
```sql
DECLARE @UserId INT = 1
DECLARE @MaxAttempts INT = 3
DECLARE @Attempt INT = 1
DECLARE @ResetSuccessful BIT = 0

WHILE @Attempt <= @MaxAttempts AND @ResetSuccessful = 0
BEGIN
    BEGIN TRY
        UPDATE Users 
        SET PasswordResetToken = NEWID(),
            TokenExpiry = DATEADD(HOUR, 1, GETDATE())
        WHERE UserId = @UserId
        
        SET @ResetSuccessful = 1
        PRINT 'Password reset token generated successfully for Ahmed Elsayyad'
    END TRY
    BEGIN CATCH
        SET @Attempt = @Attempt + 1
        IF @Attempt > @MaxAttempts
            PRINT 'Password reset failed after ' + CAST(@MaxAttempts AS VARCHAR) + ' attempts'
    END CATCH
END
```

### 3. Bulk Data Processing
```sql
DECLARE @BatchSize INT = 100
DECLARE @Processed INT = 0
DECLARE @TotalRecords INT = (SELECT COUNT(*) FROM Employees WHERE LastUpdated IS NULL)

WHILE @Processed < @TotalRecords
BEGIN
    UPDATE TOP (@BatchSize) Employees
    SET LastUpdated = GETDATE()
    WHERE LastUpdated IS NULL
    
    SET @Processed = @Processed + @@ROWCOUNT
    PRINT 'Processed ' + CAST(@Processed AS VARCHAR) + ' of ' + CAST(@TotalRecords AS VARCHAR) + ' records'
    
    WAITFOR DELAY '00:00:01' -- Prevent resource overload
END
```

### 4. Security - Brute Force Protection
```sql
DECLARE @Username NVARCHAR(50) = 'Mohammed_Elsayyad'
DECLARE @LockoutMinutes INT = 30
DECLARE @CheckTime DATETIME = GETDATE()

WHILE EXISTS(
    SELECT 1 FROM LoginAttempts 
    WHERE Username = @Username 
    AND AttemptTime > DATEADD(MINUTE, -@LockoutMinutes, @CheckTime)
    AND Success = 0
)
BEGIN
    PRINT 'Account temporarily locked - too many failed attempts'
    WAITFOR DELAY '00:05:00' -- Wait 5 minutes before next check
    
    SET @CheckTime = GETDATE()
END

PRINT 'Account lockout cleared for ' + @Username
```

### 5. Data Validation Loop
```sql
DECLARE @InvalidEmailCount INT = 1
DECLARE @CorrectionCount INT = 0

WHILE @InvalidEmailCount > 0
BEGIN
    -- Find and correct invalid emails
    UPDATE TOP (50) Employees
    SET Email = LOWER(FirstName) + '.' + LOWER(LastName) + '@company.com'
    WHERE Email NOT LIKE '%@%.%' 
       OR Email IS NULL
    
    SET @CorrectionCount = @CorrectionCount + @@ROWCOUNT
    
    SELECT @InvalidEmailCount = COUNT(*)
    FROM Employees
    WHERE Email NOT LIKE '%@%.%' 
       OR Email IS NULL
    
    PRINT 'Corrected ' + CAST(@CorrectionCount AS VARCHAR) + ' emails. Remaining: ' + CAST(@InvalidEmailCount AS VARCHAR)
    
    IF @InvalidEmailCount > 0
        WAITFOR DELAY '00:00:01'
END
```

## ⪢ CASE Expressions

### 1. Salary Classification
```sql
SELECT 
    EmployeeId,
    FirstName + ' ' + LastName AS EmployeeName,
    Salary,
    SalaryCategory = CASE 
        WHEN Salary >= 8000 THEN 'High Paid'
        WHEN Salary >= 4000 THEN 'Medium Paid' 
        WHEN Salary < 4000 THEN 'Low Paid'
        ELSE 'Not Specified'
    END,
    PerformanceBonus = CASE 
        WHEN Department = 'IT' THEN Salary * 0.15
        WHEN Department = 'Sales' THEN Salary * 0.20
        ELSE Salary * 0.10
    END
FROM Employees
WHERE Active = 1
ORDER BY Salary DESC
```

### 2. Security Access Levels
```sql
SELECT 
    UserId,
    Username,
    UserRole,
    AccessLevel = CASE 
        WHEN UserRole = 'Admin' THEN 'Full Access'
        WHEN UserRole = 'Manager' THEN 'Department Access'
        WHEN UserRole = 'User' THEN 'Limited Access'
        WHEN UserRole = 'Guest' THEN 'Read Only'
        ELSE 'No Access'
    END,
    SecurityClearance = CASE 
        WHEN IsActive = 1 AND LastLogin > DATEADD(MONTH, -6, GETDATE()) THEN 'Active'
        WHEN IsActive = 1 AND LastLogin <= DATEADD(MONTH, -6, GETDATE()) THEN 'Review Required'
        ELSE 'Inactive'
    END
FROM Users
```

### 3. Employee Status with Security
```sql
SELECT 
    EmployeeId,
    FirstName + ' ' + LastName AS EmployeeName,
    Department,
    CASE 
        WHEN Active = 1 AND DATEDIFF(MONTH, LastSecurityTraining, GETDATE()) <= 12 THEN 'Active - Compliant'
        WHEN Active = 1 AND DATEDIFF(MONTH, LastSecurityTraining, GETDATE()) > 12 THEN 'Active - Training Required'
        WHEN Active = 0 THEN 'Inactive'
        ELSE 'Status Unknown'
    END AS SecurityStatus,
    CASE 
        WHEN Department IN ('IT', 'Finance', 'HR') THEN 'High Security'
        WHEN Department IN ('Sales', 'Marketing') THEN 'Medium Security' 
        ELSE 'Standard Security'
    END AS DepartmentSecurityLevel
FROM Employees
```

### 4. Data Sensitivity Classification
```sql
SELECT 
    TableName,
    ColumnName,
    DataType,
    SensitivityLevel = CASE 
        WHEN ColumnName LIKE '%Password%' OR ColumnName LIKE '%Token%' THEN 'High - Encrypt'
        WHEN ColumnName LIKE '%Email%' OR ColumnName LIKE '%Phone%' THEN 'Medium - Mask'
        WHEN ColumnName LIKE '%Salary%' OR ColumnName LIKE '%Bonus%' THEN 'Medium - Restrict'
        ELSE 'Low - Standard'
    END,
    AccessControl = CASE 
        WHEN ColumnName LIKE '%Password%' THEN 'System Only'
        WHEN ColumnName LIKE '%Salary%' THEN 'HR Managers Only'
        WHEN ColumnName LIKE '%Email%' THEN 'Authenticated Users'
        ELSE 'Public'
    END
FROM Information_Schema.Columns
WHERE TableName IN ('Employees', 'Users', 'Payroll')
```

### 5. Audit Risk Assessment
```sql
SELECT 
    UserName,
    ActionType,
    TableName,
    RiskLevel = CASE 
        WHEN ActionType = 'DELETE' AND TableName IN ('Users', 'AuditLog') THEN 'CRITICAL'
        WHEN ActionType = 'UPDATE' AND TableName IN ('Employees', 'Salary') THEN 'HIGH'
        WHEN ActionType = 'INSERT' AND TableName IN ('LoginAttempts') THEN 'MEDIUM'
        ELSE 'LOW'
    END,
    ResponseAction = CASE 
        WHEN ActionType = 'DELETE' THEN 'IMMEDIATE_REVIEW'
        WHEN ActionType = 'UPDATE' AND TableName = 'Salary' THEN 'MANAGER_APPROVAL_REQUIRED'
        ELSE 'ROUTINE_MONITORING'
    END
FROM AuditTrail
WHERE EventDate > DATEADD(DAY, -1, GETDATE())
```

## ⪢ Advanced Security Examples

### 1. Secure User Authentication
```sql
CREATE PROCEDURE AuthenticateUser
    @Username NVARCHAR(50),
    @InputPassword NVARCHAR(100)
AS
BEGIN
    DECLARE @UserId INT
    DECLARE @HashedPassword VARBINARY(256)
    DECLARE @IsLocked BIT
    DECLARE @FailedAttempts INT
    
    SELECT @UserId = UserId,
           @HashedPassword = HashedPassword,
           @IsLocked = IsLocked,
           @FailedAttempts = FailedLoginAttempts
    FROM Users 
    WHERE Username = @Username
    
    IF @UserId IS NULL
    BEGIN
        PRINT 'Invalid username'
        RETURN -1
    END
    
    IF @IsLocked = 1
    BEGIN
        PRINT 'Account is locked. Contact administrator.'
        RETURN -2
    END
    
    IF @HashedPassword = HASHBYTES('SHA2_256', @InputPassword)
    BEGIN
        UPDATE Users 
        SET LastLogin = GETDATE(),
            FailedLoginAttempts = 0
        WHERE UserId = @UserId
        
        PRINT 'Login successful for ' + @Username
        RETURN 1
    END
    ELSE
    BEGIN
        UPDATE Users 
        SET FailedLoginAttempts = @FailedAttempts + 1,
            IsLocked = CASE WHEN @FailedAttempts + 1 >= 5 THEN 1 ELSE 0 END
        WHERE UserId = @UserId
        
        PRINT 'Invalid password for ' + @Username
        RETURN -3
    END
END
```

### 2. Data Encryption Example
```sql
-- Create master key and certificate for encryption
CREATE MASTER KEY ENCRYPTION BY PASSWORD = 'StrongMasterKeyPassword123!'

CREATE CERTIFICATE EmployeeDataCert
WITH SUBJECT = 'Certificate for employee data encryption'

CREATE SYMMETRIC KEY EmployeeDataKey
WITH ALGORITHM = AES_256
ENCRYPTION BY CERTIFICATE EmployeeDataCert

-- Encrypt sensitive data
OPEN SYMMETRIC KEY EmployeeDataKey DECRYPTION BY CERTIFICATE EmployeeDataCert

UPDATE Employees 
SET EncryptedSalary = EncryptByKey(Key_GUID('EmployeeDataKey'), CAST(Salary AS VARCHAR(100))),
    EncryptedSSN = EncryptByKey(Key_GUID('EmployeeDataKey'), SSN)
WHERE EmployeeId IN (SELECT EmployeeId FROM Employees WHERE Manager = 'Ahmed Elsayyad')

CLOSE SYMMETRIC KEY EmployeeDataKey
```

### 3. Audit Trail Implementation
```sql
CREATE TABLE SecurityAudit (
    AuditId INT IDENTITY PRIMARY KEY,
    UserName NVARCHAR(50),
    ActionType NVARCHAR(20),
    TableName NVARCHAR(50),
    RecordId INT,
    OldValues NVARCHAR(MAX),
    NewValues NVARCHAR(MAX),
    IpAddress NVARCHAR(45),
    AuditTimestamp DATETIME DEFAULT GETDATE()
)

CREATE TRIGGER trg_AuditEmployeeChanges
ON Employees
AFTER UPDATE, DELETE
AS
BEGIN
    SET NOCOUNT ON
    
    INSERT INTO SecurityAudit (UserName, ActionType, TableName, RecordId, OldValues, NewValues, IpAddress)
    SELECT 
        SYSTEM_USER,
        CASE WHEN EXISTS(SELECT 1 FROM inserted) THEN 'UPDATE' ELSE 'DELETE' END,
        'Employees',
        COALESCE(d.EmployeeId, i.EmployeeId),
        (SELECT * FROM deleted FOR JSON AUTO),
        (SELECT * FROM inserted FOR JSON AUTO),
        CONNECTIONPROPERTY('client_net_address')
    FROM deleted d
    FULL OUTER JOIN inserted i ON d.EmployeeId = i.EmployeeId
END
```

### 4. Row-Level Security
```sql
-- Create security policy for department-based access
CREATE SCHEMA Security
GO

CREATE FUNCTION Security.fn_EmployeeAccessPredicate(@Department NVARCHAR(50))
RETURNS TABLE
WITH SCHEMABINDING
AS
RETURN SELECT 1 AS AccessResult
FROM dbo.Employees
WHERE @Department = DEPARTMENT
   OR IS_MEMBER('db_owner') = 1
   OR USER_NAME() = 'Ahmed_Elsayyad'
GO

CREATE SECURITY POLICY EmployeeSecurityPolicy
ADD FILTER PREDICATE Security.fn_EmployeeAccessPredicate(Department)
ON dbo.Employees
WITH (STATE = ON)
```

### 5. Comprehensive Security Monitoring
```sql
CREATE PROCEDURE MonitorSecurityEvents
AS
BEGIN
    -- Check for failed logins
    IF EXISTS(
        SELECT 1 FROM LoginAttempts 
        WHERE AttemptTime > DATEADD(HOUR, -1, GETDATE()) 
        AND Success = 0 
        GROUP BY Username, IpAddress 
        HAVING COUNT(*) > 5
    )
    BEGIN
        PRINT 'SECURITY ALERT: Multiple failed login attempts detected'
        -- Send alert to security team
    END
    
    -- Check for unauthorized data access
    IF EXISTS(
        SELECT 1 FROM SecurityAudit 
        WHERE AuditTimestamp > DATEADD(HOUR, -1, GETDATE())
        AND UserName NOT IN ('Ahmed_Elsayyad', 'Mohammed_Elsayyad')
        AND ActionType = 'SELECT'
        AND TableName IN ('Salary', 'Users', 'AuditTrail')
    )
    BEGIN
        PRINT 'SECURITY ALERT: Unauthorized data access detected'
        -- Trigger security investigation
    END
    
    -- Check for suspicious bulk operations
    IF EXISTS(
        SELECT 1 FROM SecurityAudit 
        WHERE AuditTimestamp > DATEADD(MINUTE, -5, GETDATE())
        AND ActionType IN ('DELETE', 'UPDATE')
        GROUP BY UserName, TableName
        HAVING COUNT(*) > 100
    )
    BEGIN
        PRINT 'SECURITY ALERT: Suspicious bulk operations detected'
        -- Freeze user account and notify admin
    END
END
```

## ⪢ T-SQL Programming Basics

### 1. Variable Declaration and Initialization
```sql
DECLARE @EmployeeName VARCHAR(100) = 'Ahmed Elsayyad'
DECLARE @Salary DECIMAL(10,2), @TaxRate FLOAT, @NetSalary DECIMAL(10,2)
SET @Salary = 7500.00
SET @TaxRate = 0.15
SET @NetSalary = @Salary - (@Salary * @TaxRate)
SELECT @EmployeeName AS Name, @NetSalary AS NetSalary
```

### 2. IF...ELSE with Multiple Conditions
```sql
DECLARE @Grade CHAR(1) = 'B'
DECLARE @GradePoints DECIMAL(3,2)

IF @Grade = 'A'
    SET @GradePoints = 4.0
ELSE IF @Grade = 'B'
    SET @GradePoints = 3.0
ELSE IF @Grade = 'C'
    SET @GradePoints = 2.0
ELSE IF @Grade = 'D'
    SET @GradePoints = 1.0
ELSE
    SET @GradePoints = 0.0

PRINT 'Grade Points: ' + CAST(@GradePoints AS VARCHAR)
```

### 3. WHILE Loop with BREAK and CONTINUE
```sql
DECLARE @Counter INT = 1
DECLARE @Total INT = 0

WHILE @Counter <= 100
BEGIN
    SET @Total = @Total + @Counter
    SET @Counter = @Counter + 1
    
    IF @Counter = 50
        CONTINUE  -- Skip the rest and continue loop
    
    IF @Total > 2000
        BREAK  -- Exit loop
    
    PRINT 'Counter: ' + CAST(@Counter AS VARCHAR) + ', Total: ' + CAST(@Total AS VARCHAR)
END
```

### 4. CASE Expression in SELECT
```sql
SELECT 
    EmployeeID,
    EmployeeName = FirstName + ' ' + LastName,
    Salary,
    SalaryCategory = CASE 
        WHEN Salary >= 8000 THEN 'High Paid'
        WHEN Salary >= 4000 THEN 'Medium Paid'
        WHEN Salary < 4000 THEN 'Low Paid'
        ELSE 'Undefined'
    END,
    DepartmentBonus = CASE DepartmentID
        WHEN 1 THEN Salary * 0.15  -- IT Department
        WHEN 2 THEN Salary * 0.10  -- HR Department
        WHEN 3 THEN Salary * 0.12  -- Finance Department
        ELSE Salary * 0.05
    END
FROM Employees
WHERE Active = 1
```

### 5. Dynamic SQL Execution
```sql
DECLARE @TableName NVARCHAR(50) = 'Employees'
DECLARE @ColumnName NVARCHAR(50) = 'Salary'
DECLARE @Department NVARCHAR(50) = 'IT'
DECLARE @SQL NVARCHAR(1000)

SET @SQL = 'SELECT AVG(' + @ColumnName + ') AS AverageSalary FROM ' + @TableName + 
           ' WHERE Department = @DeptName'

EXEC sp_executesql @SQL, N'@DeptName NVARCHAR(50)', @DeptName = @Department
```

## ⪢ User-Defined Functions

### 1. Scalar Function - Calculate Age
```sql
CREATE FUNCTION CalculateAge(@BirthDate DATE)
RETURNS INT
AS
BEGIN
    DECLARE @Age INT
    SET @Age = DATEDIFF(YEAR, @BirthDate, GETDATE())
    
    IF (MONTH(@BirthDate) > MONTH(GETDATE()) OR 
        (MONTH(@BirthDate) = MONTH(GETDATE()) AND DAY(@BirthDate) > DAY(GETDATE())))
        SET @Age = @Age - 1
        
    RETURN @Age
END

-- Usage
SELECT dbo.CalculateAge('1990-05-15') AS Age
```

### 2. Scalar Function - Tax Calculation
```sql
CREATE FUNCTION CalculateTax(@Salary DECIMAL(10,2))
RETURNS DECIMAL(10,2)
AS
BEGIN
    DECLARE @Tax DECIMAL(10,2) = 0
    
    IF @Salary > 5000
        SET @Tax = (@Salary - 5000) * 0.20 + 500  -- 20% on amount above 5000 + fixed 500
    ELSE IF @Salary > 3000
        SET @Tax = (@Salary - 3000) * 0.15  -- 15% on amount above 3000
    ELSE IF @Salary > 1500
        SET @Tax = (@Salary - 1500) * 0.10  -- 10% on amount above 1500
    -- Below 1500: No tax
    
    RETURN @Tax
END

-- Usage
SELECT EmployeeName, Salary, dbo.CalculateTax(Salary) AS TaxAmount FROM Employees
```

### 3. Inline Table-Valued Function - Department Employees
```sql
CREATE FUNCTION GetDepartmentEmployees(@DepartmentName VARCHAR(50))
RETURNS TABLE
AS
RETURN (
    SELECT 
        EmployeeID,
        EmployeeName = FirstName + ' ' + LastName,
        Position,
        Salary,
        HireDate
    FROM Employees
    WHERE Department = @DepartmentName 
        AND Active = 1
)

-- Usage
SELECT * FROM dbo.GetDepartmentEmployees('IT')
```

### 4. Inline Table-Valued Function - Employee Search
```sql
CREATE FUNCTION SearchEmployees(@SearchTerm VARCHAR(100))
RETURNS TABLE
AS
RETURN (
    SELECT 
        EmployeeID,
        EmployeeName,
        Department,
        Position,
        Email
    FROM Employees
    WHERE EmployeeName LIKE '%' + @SearchTerm + '%'
        OR Department LIKE '%' + @SearchTerm + '%'
        OR Position LIKE '%' + @SearchTerm + '%'
)

-- Usage
SELECT * FROM dbo.SearchEmployees('Manager')
```

### 5. Multi-Statement Table-Valued Function - Employee Hierarchy
```sql
CREATE FUNCTION GetEmployeeHierarchy(@ManagerID INT)
RETURNS @Hierarchy TABLE (
    EmployeeID INT,
    EmployeeName VARCHAR(100),
    Position VARCHAR(50),
    Level INT,
    HierarchyPath VARCHAR(500)
)
AS
BEGIN
    DECLARE @Level INT = 1
    
    -- Insert direct reports
    INSERT INTO @Hierarchy
    SELECT 
        EmployeeID,
        EmployeeName,
        Position,
        @Level,
        CAST(EmployeeName AS VARCHAR(500))
    FROM Employees
    WHERE ManagerID = @ManagerID
    
    -- Insert second level reports
    INSERT INTO @Hierarchy
    SELECT 
        e.EmployeeID,
        e.EmployeeName,
        e.Position,
        @Level + 1,
        h.HierarchyPath + ' -> ' + e.EmployeeName
    FROM Employees e
    INNER JOIN @Hierarchy h ON e.ManagerID = h.EmployeeID
    
    RETURN
END

-- Usage
SELECT * FROM dbo.GetEmployeeHierarchy(101)
```

## ⪢ Stored Procedures

### 1. Basic Stored Procedure with Parameters
```sql
CREATE PROCEDURE GetEmployeeByDepartment
    @DepartmentName VARCHAR(50),
    @MinSalary DECIMAL(10,2) = 0
AS
BEGIN
    SELECT 
        EmployeeID,
        EmployeeName,
        Position,
        Salary,
        Email
    FROM Employees
    WHERE Department = @DepartmentName
        AND Salary >= @MinSalary
        AND Active = 1
    ORDER BY Salary DESC
END

-- Execution
EXEC GetEmployeeByDepartment @DepartmentName = 'IT', @MinSalary = 5000
EXEC GetEmployeeByDepartment 'Finance'  -- Using default MinSalary
```

### 2. Stored Procedure with OUTPUT Parameters
```sql
CREATE PROCEDURE GetDepartmentStatistics
    @DepartmentName VARCHAR(50),
    @EmployeeCount INT OUTPUT,
    @TotalSalary DECIMAL(15,2) OUTPUT,
    @AverageSalary DECIMAL(10,2) OUTPUT
AS
BEGIN
    SELECT 
        @EmployeeCount = COUNT(*),
        @TotalSalary = SUM(Salary),
        @AverageSalary = AVG(Salary)
    FROM Employees
    WHERE Department = @DepartmentName
        AND Active = 1
    
    IF @EmployeeCount = 0
    BEGIN
        SET @TotalSalary = 0
        SET @AverageSalary = 0
    END
END

-- Execution with OUTPUT parameters
DECLARE @Count INT, @Total DECIMAL(15,2), @Avg DECIMAL(10,2)
EXEC GetDepartmentStatistics 'IT', @Count OUTPUT, @Total OUTPUT, @Avg OUTPUT
SELECT @Count AS EmployeeCount, @Total AS TotalSalary, @Avg AS AverageSalary
```

### 3. Stored Procedure with Transaction
```sql
CREATE PROCEDURE TransferEmployee
    @EmployeeID INT,
    @NewDepartment VARCHAR(50),
    @NewSalary DECIMAL(10,2)
AS
BEGIN
    SET NOCOUNT ON
    BEGIN TRY
        BEGIN TRANSACTION
        
        -- Update employee department and salary
        UPDATE Employees 
        SET Department = @NewDepartment, 
            Salary = @NewSalary,
            LastModified = GETDATE()
        WHERE EmployeeID = @EmployeeID
        
        -- Log the transfer
        INSERT INTO EmployeeTransfers (EmployeeID, FromDepartment, ToDepartment, TransferDate, NewSalary)
        SELECT 
            @EmployeeID,
            Department,
            @NewDepartment,
            GETDATE(),
            @NewSalary
        FROM Employees 
        WHERE EmployeeID = @EmployeeID
        
        COMMIT TRANSACTION
        PRINT 'Employee transfer completed successfully'
    END TRY
    BEGIN CATCH
        ROLLBACK TRANSACTION
        DECLARE @ErrorMessage NVARCHAR(4000) = ERROR_MESSAGE()
        RAISERROR('Transfer failed: %s', 16, 1, @ErrorMessage)
    END CATCH
END
```

### 4. Stored Procedure with Conditional Logic
```sql
CREATE PROCEDURE UpdateEmployeeSalary
    @EmployeeID INT,
    @IncreasePercentage DECIMAL(5,2),
    @Reason VARCHAR(200)
AS
BEGIN
    DECLARE @CurrentSalary DECIMAL(10,2)
    DECLARE @NewSalary DECIMAL(10,2)
    DECLARE @MaxSalary DECIMAL(10,2) = 20000
    
    -- Get current salary
    SELECT @CurrentSalary = Salary 
    FROM Employees 
    WHERE EmployeeID = @EmployeeID
    
    IF @CurrentSalary IS NULL
    BEGIN
        RAISERROR('Employee not found', 16, 1)
        RETURN
    END
    
    -- Calculate new salary
    SET @NewSalary = @CurrentSalary * (1 + @IncreasePercentage/100)
    
    -- Apply cap if necessary
    IF @NewSalary > @MaxSalary
    BEGIN
        SET @NewSalary = @MaxSalary
        PRINT 'Salary capped at maximum limit of ' + CAST(@MaxSalary AS VARCHAR)
    END
    
    -- Update salary
    UPDATE Employees 
    SET Salary = @NewSalary,
        LastSalaryIncrease = GETDATE()
    WHERE EmployeeID = @EmployeeID
    
    -- Log salary change
    INSERT INTO SalaryHistory (EmployeeID, OldSalary, NewSalary, IncreasePercent, Reason, ChangeDate)
    VALUES (@EmployeeID, @CurrentSalary, @NewSalary, @IncreasePercentage, @Reason, GETDATE())
    
    SELECT 
        @EmployeeID AS EmployeeID,
        @CurrentSalary AS OldSalary,
        @NewSalary AS NewSalary,
        (@NewSalary - @CurrentSalary) AS IncreaseAmount
END
```

### 5. Stored Procedure for Bulk Operations
```sql
CREATE PROCEDURE BulkUpdateSalaries
    @DepartmentName VARCHAR(50),
    @IncreasePercentage DECIMAL(5,2)
AS
BEGIN
    BEGIN TRY
        BEGIN TRANSACTION
        
        -- Update salaries for the department
        UPDATE Employees 
        SET Salary = Salary * (1 + @IncreasePercentage/100),
            LastModified = GETDATE()
        WHERE Department = @DepartmentName
            AND Active = 1
        
        -- Log bulk update
        INSERT INTO BulkSalaryUpdates (Department, IncreasePercentage, AffectedEmployees, UpdateDate)
        SELECT 
            @DepartmentName,
            @IncreasePercentage,
            @@ROWCOUNT,
            GETDATE()
        
        COMMIT TRANSACTION
        
        PRINT 'Successfully updated salaries for ' + CAST(@@ROWCOUNT AS VARCHAR) + ' employees'
    END TRY
    BEGIN CATCH
        ROLLBACK TRANSACTION
        DECLARE @ErrorMsg NVARCHAR(4000) = ERROR_MESSAGE()
        RAISERROR('Bulk update failed: %s', 16, 1, @ErrorMsg)
    END CATCH
END
```

## ⪢ Triggers

### 1. AFTER INSERT Trigger for Audit
```sql
CREATE TRIGGER trg_AuditEmployeeInsert
ON Employees
AFTER INSERT
AS
BEGIN
    SET NOCOUNT ON
    
    INSERT INTO EmployeeAudit (
        EmployeeID, 
        Action, 
        ActionDate, 
        UserName, 
        Details
    )
    SELECT 
        i.EmployeeID,
        'INSERT',
        GETDATE(),
        SYSTEM_USER,
        'New employee: ' + i.FirstName + ' ' + i.LastName + 
        ', Department: ' + i.Department + 
        ', Salary: ' + CAST(i.Salary AS VARCHAR)
    FROM inserted i
END
```

### 2. AFTER UPDATE Trigger for History Tracking
```sql
CREATE TRIGGER trg_TrackEmployeeChanges
ON Employees
AFTER UPDATE
AS
BEGIN
    SET NOCOUNT ON
    
    -- Track salary changes
    IF UPDATE(Salary)
    BEGIN
        INSERT INTO SalaryHistory (
            EmployeeID, 
            OldSalary, 
            NewSalary, 
            ChangeDate, 
            ChangedBy
        )
        SELECT 
            i.EmployeeID,
            d.Salary,
            i.Salary,
            GETDATE(),
            SYSTEM_USER
        FROM inserted i
        INNER JOIN deleted d ON i.EmployeeID = d.EmployeeID
        WHERE i.Salary <> d.Salary
    END
    
    -- Track department changes
    IF UPDATE(Department)
    BEGIN
        INSERT INTO DepartmentHistory (
            EmployeeID, 
            OldDepartment, 
            NewDepartment, 
            ChangeDate, 
            ChangedBy
        )
        SELECT 
            i.EmployeeID,
            d.Department,
            i.Department,
            GETDATE(),
            SYSTEM_USER
        FROM inserted i
        INNER JOIN deleted d ON i.EmployeeID = d.EmployeeID
        WHERE i.Department <> d.Department
    END
END
```

### 3. INSTEAD OF DELETE Trigger for Soft Delete
```sql
CREATE TRIGGER trg_SoftDeleteEmployee
ON Employees
INSTEAD OF DELETE
AS
BEGIN
    SET NOCOUNT ON
    
    -- Update active status instead of deleting
    UPDATE e
    SET e.Active = 0,
        e.TerminationDate = GETDATE(),
        e.LastModified = GETDATE()
    FROM Employees e
    INNER JOIN deleted d ON e.EmployeeID = d.EmployeeID
    WHERE e.Active = 1
    
    -- Archive the deleted records
    INSERT INTO EmployeeArchive
    SELECT *, GETDATE() 
    FROM deleted
    
    PRINT 'Employees soft deleted successfully'
END
```

### 4. DDL Trigger for Schema Changes
```sql
CREATE TRIGGER trg_AuditSchemaChanges
ON DATABASE
FOR CREATE_TABLE, ALTER_TABLE, DROP_TABLE
AS
BEGIN
    SET NOCOUNT ON
    
    DECLARE @EventData XML = EVENTDATA()
    
    INSERT INTO SchemaAudit (
        EventType,
        ObjectName,
        ObjectType,
        SQLCommand,
        UserName,
        EventDate
    )
    VALUES (
        @EventData.value('(/EVENT_INSTANCE/EventType)[1]', 'NVARCHAR(100)'),
        @EventData.value('(/EVENT_INSTANCE/ObjectName)[1]', 'NVARCHAR(100)'),
        @EventData.value('(/EVENT_INSTANCE/ObjectType)[1]', 'NVARCHAR(100)'),
        @EventData.value('(/EVENT_INSTANCE/TSQLCommand/CommandText)[1]', 'NVARCHAR(MAX)'),
        SYSTEM_USER,
        GETDATE()
    )
    
    PRINT 'Schema change has been audited'
END
```

### 5. Conditional Trigger with Validation
```sql
CREATE TRIGGER trg_ValidateSalaryUpdate
ON Employees
FOR UPDATE
AS
BEGIN
    SET NOCOUNT ON
    
    -- Check if salary is being decreased
    IF EXISTS (
        SELECT 1 
        FROM inserted i
        INNER JOIN deleted d ON i.EmployeeID = d.EmployeeID
        WHERE i.Salary < d.Salary
    )
    BEGIN
        RAISERROR('Salary decrease is not allowed. Use the dedicated procedure for salary adjustments.', 16, 1)
        ROLLBACK TRANSACTION
        RETURN
    END
    
    -- Check if salary exceeds department maximum
    IF EXISTS (
        SELECT 1 
        FROM inserted i
        INNER JOIN Departments d ON i.Department = d.DepartmentName
        WHERE i.Salary > d.MaxSalary
    )
    BEGIN
        RAISERROR('Salary exceeds department maximum limit.', 16, 1)
        ROLLBACK TRANSACTION
        RETURN
    END
END
```

